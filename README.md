# Dynamic Language Mode for TYPO3

Applies `fallbackType = free` as translations mode, when free translations are found on the currently visited page translation.

## Installation

```
composer require qbus/dlm
```
